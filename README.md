# API Documentation

Each **API** folder contains an API collection with corresponding tests.

## brow-cli

`brow-cli` is a command-line interface tool for authenticating with Collaboration Hub. The CLI automatically extracts environment variables required for API testing and injects them into the respective environments of each Bruno collection.

more at [quick-start-bro-cli.md](quick-start-bro-cli.md)